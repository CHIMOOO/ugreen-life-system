{
  "name": "choujiang",
  "version": "1.0.0",
  "private": true,
  "description": "幸运抽奖 & 下午茶系统（前台 web + 后台 admin + node 服务）",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install && npm --prefix admin install",
    "seed": "npm --prefix server run seed",
    "server": "npm --prefix server start",
    "web": "npm --prefix web run dev",
    "admin": "npm --prefix admin run dev",
    "all": "node scripts/start-all.mjs",
    "build": "npm --prefix web run build && npm --prefix admin run build"
  }
}
